# Benchmarks

This folder will store reproducible benchmark submissions.

Recommended fields:

- device and GPU ID
- model ID
- quantization
- runtime
- tokens per second
- prompt length
- output length
- power draw if available
- source or submitter
- confidence level

Benchmarks should never be mixed with official specs. They are observational data and should keep methodology visible.
